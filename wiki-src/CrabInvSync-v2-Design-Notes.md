# CrabInvSync v2 Design Notes

CrabInvSync v2 should only build on access paths that CrabRuntimeProbe has confirmed with scoped runtime evidence. This staging page is documentation only and does not add gameplay sync.
