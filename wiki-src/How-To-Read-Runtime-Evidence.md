# How To Read Runtime Evidence

Runtime evidence is scoped. SAFE means a specific access method returned safely in the recorded context, role, and lifecycle state.

Objectdump discovery only means the symbol exists in static dump data.
