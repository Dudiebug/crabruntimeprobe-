# Object Dump Inputs

Place Crab Champions UE4SS object dump files here.

Accepted input patterns for parser tooling:

- `*.txt`
- `*.part*`
- `*.md`

The parser is fail-soft and preserves raw matched lines when formatting is unknown.
